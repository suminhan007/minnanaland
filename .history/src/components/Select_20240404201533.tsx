import React from "react";
import styled from "styled-components";

type SelectProps = {};
const Select: React.FC<SelectProps> = ({}) => {
  return <StyleSelectWrap>Select</StyleSelectWrap>;
};

const StyleSelectWrap = styled
export default Select;
