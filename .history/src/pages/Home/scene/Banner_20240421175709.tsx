import React, { useEffect, useRef } from "react";

type Props = {};
const Banner: React.FC<Props> = ({}) => {

    return <Canvas>
        <mesh>
            
      </mesh>
  </Canvas>;
};
export default Banner;
