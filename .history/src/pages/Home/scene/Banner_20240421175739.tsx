import React from "react";
import {Canvas} from '@'

type Props = {};
const Banner: React.FC<Props> = ({}) => {
  return (
    <Canvas>
      <mesh></mesh>
    </Canvas>
  );
};
export default Banner;
