import React from "react";

type Props = {
    data?: {name:string,icon:string|React.ReactNode}[];
};

const Application: React.FC<Props> = ({}) => {
  return <div>Application</div>;
};
export default Application;
