#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/suminhan/Documents/project/minna's projects/minnanaland/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/suminhan/Documents/project/minna's projects/minnanaland/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../@microsoft+api-extractor@7.39.0_@types+node@20.11.16/node_modules/@microsoft/api-extractor/bin/api-extractor" "$@"
else
  exec node  "$basedir/../../../../../@microsoft+api-extractor@7.39.0_@types+node@20.11.16/node_modules/@microsoft/api-extractor/bin/api-extractor" "$@"
fi
